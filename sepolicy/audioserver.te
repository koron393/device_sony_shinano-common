allow audioserver mpctl_socket:dir r_dir_perms;
allow audioserver mpctl_socket:sock_file w_file_perms;

# access to perflock
unix_socket_connect(audioserver, mpctl, perfd)
unix_socket_send(audioserver, mpctl, perfd)
